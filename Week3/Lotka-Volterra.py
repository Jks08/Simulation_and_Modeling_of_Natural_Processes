#For the following project, on Windows, first install on setup MikTex and then run the code in your command prompt. 
#This will display a bunch of images each corresponding to a set of Array values. Infer the required output from that.

import numpy as np
import math
import matplotlib.pyplot as plt
